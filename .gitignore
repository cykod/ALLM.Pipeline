# Build artifacts
/_build/
/deps/
/doc/
/cover/

# Generated on crash by the VM
erl_crash.dump

# Elixir language server
/.elixir_ls/
/.lexical/

# Editor/OS noise
.DS_Store

# Local environment files (secrets never belong in the repo)
.env
.env.*

# `mix hex.build` output
*.tar

# Devcontainer service data lives in named volumes; nothing local to ignore,
# but keep the Hex auth file out if someone copies it in for a release
/.hex/
